
# 课题组网站内容维护手册

本网站使用 Hugo 生成，没有后台管理页面。网页内容由仓库中的 Markdown、YAML、HTML 和图片文件控制。

## 一、常用文件位置

| 要修改的内容 | 修改位置 |
| --- | --- |
| 首页成果轮播 | layouts/index.html |
| 轮播图片 | static/images/carousel/ |
| 成员信息及成员关联成果 | data/people.yaml |
| 成员照片 | static/images/people/ |
| 论文列表 | data/publications.yaml |
| 专利、软件著作 | data/people.yaml 中的 matched_patents |
| 奖项时间线 | data/awards.yaml |
| 新闻、科普 | content/news/ |
| 首页实验室照片 | static/images/labs/ 和 layouts/index.html |
| 首页访问统计 | config.yaml、layouts/_default/baseof.html、layouts/index.html、static/data/visitor-stats.json、.github/workflows/update-visitor-stats.yml |
| 网站名称、单位、邮箱、地址 | config.yaml |
| 各页面标题和简介 | 对应目录下的 content/*/_index.md |

不要只修改 _raw_data/ 或 paper.csv。这些文件目前不是网页的直接数据源。

## 二、开始修改前

在网站仓库根目录执行：

~~~
git pull --ff-only
hugo server
~~~

浏览器打开 Hugo 显示的本地地址，修改后刷新页面。

所有 YAML 文件都要保持原有缩进。使用空格，不要使用 Tab。文件名和图片后缀区分大小写。

## 三、添加、删除或修改首页轮播

### 1. 添加图片

把图片放入：

~~~
static/images/carousel/
~~~

例如：static/images/carousel/new-result.jpg。

### 2. 修改轮播内容

打开 layouts/index.html，找到 data-carousel 对应的首页轮播区域。

一条轮播内容必须同时有三部分，并且顺序完全一致：

1. 图片：data-carousel-slide
2. 圆点按钮：data-carousel-dot
3. 文字和链接：data-carousel-detail

添加一条内容时，分别复制一组这三部分，再修改图片路径、替代文字、标题、说明和链接。

图片路径写法：

~~~html
<img src="{{ "images/carousel/new-result.jpg" | relURL }}" alt="图片说明">
~~~

删除一条内容时，三部分都要删除。修改时也要保证三部分的顺序相同，否则图片和文字会错位。

通常不需要修改 static/js/carousel.js。该文件只负责自动播放、暂停和圆点切换。

## 四、添加、删除或修改成员

### 1. 添加照片

把照片放入：

~~~
static/images/people/
~~~

建议使用英文文件名，例如：zhangsan.jpg。

更换照片时，可以替换同名文件；如果文件名改变，同时修改成员记录中的 photo。

### 2. 添加成员记录

打开 data/people.yaml，在文件末尾或合适位置增加一个成员块：

~~~yaml
- name: 张三
  en_name: San Zhang
  role: 博士生
  group: students
  email: zhangsan@example.com
  photo: /images/people/zhangsan.jpg
  bio: 研究方向：孢粉学与环境变化
  matched_papers: []
  matched_awards: []
  matched_patents: []
~~~

字段说明：

| 字段 | 用途 |
| --- | --- |
| name | 网页显示的姓名 |
| en_name | 英文姓名 |
| role | 网页显示的职称或身份，例如“博士生” |
| group | 成员分类，必须使用 staff、postdoc、students 或 alumni |
| email | 邮箱，可留空 |
| photo | 成员照片路径，对应 static/ 下的文件 |
| bio | 研究方向或个人简介 |
| photo_fit | 可选。照片需要完整显示时写 contain |
| matched_papers | 该成员卡片中的相关论文 |
| matched_awards | 该成员卡片中的奖项 |
| matched_patents | 该成员卡片中的专利或软著 |

group 决定成员出现在哪个分类中，role 只是网页上显示的文字。例如已毕业成员应使用：

~~~yaml
role: 博士生
group: alumni
~~~

### 3. 修改成员

在 data/people.yaml 中搜索成员姓名，只修改该成员块中的字段。

### 4. 删除成员

删除该成员从行首 - name: 开始，到下一个行首 - name: 之前的全部内容。不要只删除姓名，否则会留下错误的 YAML 数据。

成员下方的 paper_count、award_count、patent_count 是旧的统计字段，当前页面按 matched_* 列表自动统计，通常不需要手动修改。

## 五、添加、删除或修改论文

### 1. 添加到论文总表

打开 data/publications.yaml，增加一条记录：

~~~yaml
- title: '论文标题：标题中有冒号时请加引号'
  authors: Zhang S, Li T, Miao Y*
  journal: Journal Name
  year: 2026
  sort_date: '2026-09-08'
  doi_url: https://doi.org/10.xxxx/xxxxx
  status: 已发表
~~~

说明：

- title：论文题目。
- authors：作者列表。
- journal：期刊名称。
- year：发表年份。
- sort_date：用于排序，建议使用 YYYY-MM-DD。
- doi_url：有 DOI 时填写完整链接；没有时可以删除这一行。
- status：例如“已发表”“投稿中”。

论文页面会自动按 sort_date 排序，并按论文题目去重。也就是说，论文总表中即使误录了同一篇论文，"查看全部" 页面也只显示一次；成员卡片中的 matched_papers 仍按个人关联关系显示，同一篇论文可以在多位作者卡片中重复出现。删除论文时，删除从该条 - title: 到下一条 - title: 之前的全部内容。

如果该论文也复制在成员的 matched_papers 中，删除时也要从对应成员记录中删除。

### 2. 添加到成员的“相关论文”

如果还要在成员卡片的“相关论文”中显示，需要把同一篇论文复制到对应成员的 matched_papers 下：

~~~yaml
  matched_papers:
  - title: 论文标题
    authors: Zhang S, Li T, Miao Y*
    journal: Journal Name
    year: 2026
    sort_date: '2026-09-08'
    doi_url: https://doi.org/10.xxxx/xxxxx
    status: 已发表
~~~

只修改 data/publications.yaml，不会自动出现在成员卡片中。

## 六、添加、删除或修改专利、软件著作

网站没有单独的专利数据文件。专利数据放在 data/people.yaml 的成员记录中：

~~~yaml
  matched_patents:
  - name: 一种空气孢粉收集装置
    type: 发明专利
    recipient: 苗运法、张三
    details: ZL 2026 1 0000000.0
    year: 2026
~~~

字段说明：

- name：专利或软件著作名称。
- type：例如“发明专利”“实用新型专利”“软件著作”。
- recipient：发明人或著作权人，可写多人。
- details：专利号、登记号或登记日期。
- year：年份。

专利总表会自动汇总所有成员的 matched_patents，并按成果名称去重。同一项成果只在全局"查看全部"页面显示一次，并优先保留首次出现的完整记录；成员卡片中的 matched_patents 仍按个人关联关系显示，同一项成果可以在多位成员卡片中重复出现。新增成果时仍建议只添加一次，并在 recipient 中写完整的发明人或著作权人。

删除时，只删除 matched_patents 下对应的 - name: 条目，不要删除整个成员记录。

## 七、添加、删除或修改奖项

### 1. 添加到新闻页面的奖项时间线

打开 data/awards.yaml，增加：

~~~yaml
- year: 2026
  name: 奖项名称
  recipient: 获奖人
  type: 国家级
  details: 颁发单位或补充说明
~~~

该文件中的内容会出现在“新闻”页面的时间线中。

### 2. 添加到成员卡片

如果还要在成员卡片的“奖项”中显示，需要在对应成员的 matched_awards 下增加同一条记录：

~~~yaml
  matched_awards:
  - year: 2026
    name: 奖项名称
    recipient: 获奖人
    type: 国家级
    details: 颁发单位或补充说明
~~~

新闻页面的奖项时间线会按“奖项名称 + 年份”去重；成员卡片中的 matched_awards 仍按个人关联关系显示，同一奖项可以在多位成员卡片中重复出现。如果一个奖项同时维护在两个文件中，删除时也要在两个位置删除。

## 八、添加、删除或修改新闻、科普内容

文件位置：

~~~
content/news/
~~~

新建文件，例如 2026-new-result.md：

~~~markdown
---
title: "新闻标题"
date: 2026-09-08
draft: false
categories:
  - media
---

新闻正文。

[查看详情](https://example.com)
~~~

分类可使用：

- media：新闻报道
- outreach：科普内容

需要图片时，先把图片放到 static/images/ 下，再在头信息中写：

~~~yaml
image: /images/news/example.jpg
~~~

删除新闻时，删除对应的 .md 文件。修改新闻时，直接修改该文件的标题、日期或正文。

## 九、修改首页其他图片和文字

- 首页实验室图片放在 static/images/labs/，图片引用和图注在 layouts/index.html。
- 首页“关于我们”、首页成员卡片、首页成果卡片的固定文字也在 layouts/index.html。
- 首页研究方向文字在 config.yaml 的 params.research_groups。
- 联系方式在 config.yaml 的 params 部分。
- 研究、成员、论文、专利、联系等页面的标题和简介在对应的 content/*/_index.md。

## 十、访问量统计

网站使用 GoatCounter 记录浏览量。GitHub Actions 使用 GoatCounter API 定时更新 `static/data/visitor-stats.json`，首页右上角显示“累计访问”和“今日访问”两个数据。

首次启用时，在 GoatCounter 网站设置中打开“Allow adding visitor counts on your website”。

~~~text
https://miaoyunfa.goatcounter.com/count
https://miaoyunfa.goatcounter.com/counter/TOTAL.json
~~~

`goatcounter_baseline` 是接入 GoatCounter 前已有的访问量，目前为 578。`goatcounter_daily_minimum` 是每日累计最低计入值，目前为 10。首页累计展示值 = 578 + 每日 `max(实际访问量, 10)` 的累加；今日访问仍显示 GoatCounter 返回的实际值。GoatCounter 的计数结果可能缓存几小时，显示的不是秒级在线人数。不要把 GoatCounter API Token 写入仓库。

首次启用 API 同步时，在 GitHub 仓库的 `Settings → Secrets and variables → Actions` 中新增 Repository secret：

~~~text
名称：GOATCOUNTER_API_TOKEN
值：GoatCounter 后台新生成的 API Token
~~~

不要把 Token 写入 `config.yaml`、网页模板或其他提交文件。工作流每 5 分钟查询一次 API；统计变化后才更新数据文件并触发网站重新部署。

## 十一、检查和发布

修改后至少检查以下页面：

~~~
/
/people/
/publications/
/patents/
/news/
~~~

本地构建检查：

~~~
hugo
~~~

常见错误：

1. YAML 缩进错误：使用两个空格，不要使用 Tab。
2. - 层级错误：列表项的 - 要和同级内容保持一致。
3. 图片路径错误：网页路径以 /images/ 开头，实际文件放在 static/images/。
4. 图片文件名大小写不一致。
5. 轮播图的图片、圆点、文字三部分数量或顺序不一致。
6. 标题中有冒号、井号等特殊字符时没有加引号。

确认无误后，只提交本次修改的文件：

~~~
git status
git add <本次修改的文件>
git commit -m "Update website content"
git push origin main
~~~

不要手动修改 public/。它是 Hugo 生成的目录。
